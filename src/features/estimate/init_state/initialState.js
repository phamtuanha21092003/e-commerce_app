// import axiosInstance from "../../../axios"

// export default async function initialState() {
//   const { products } = await axiosInstance.get("/products")
//   products.map((product) => ({ id: product.id, star: 0, rating: 0 }))
// }
