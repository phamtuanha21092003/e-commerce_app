// import { createAsyncThunk, createSlice } from "@reduxjs/toolkit"
// import axiosInstance from "../../axios"

// export const fetchProduct = createAsyncThunk(
//   "estimate/fetchProduct",
//   async () => {
//     const product = await axiosInstance.get("/products").products
//     // return product.map((product) => {
//     //   id: product.id, title, price, description, discountPercentage, id, images
//     // })
//     return product
//   }
// )
// const slice = createSlice({ initialState: [], name: "estimate" })

// export default slice.reducer
